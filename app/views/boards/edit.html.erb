<div class="d-flex align-items-center">
  <h1>記事編集</h1>
  <div class="ms-auto boards__linkBox">
    <%= link_to '一覧', boards_path, class: 'btn btn-primary' %>
  </div>
  <div class="ms-auto boards__linkBox">
      <%= button_to '削除', board_path, class: 'btn btn-danger', method: :delete %>
  </div>
</div>
<%= render partial: 'form', locals: { board: @board} %>
<div class="p-comment__list">
  <div class="p-comment__listTitle">コメント</div>
  <%= render @board.comments %>
</div>